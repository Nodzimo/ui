interface:
  display_name: "Theme Token Adapter"
  short_description: "Adapt component styles to NUI theme tokens"
  default_prompt: "Use $theme-token-adapter to adapt component styles to the NUI-prefixed theme-token contract while preserving behavior."
